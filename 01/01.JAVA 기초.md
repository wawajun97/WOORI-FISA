# ☕ Java 핵심 정리

## 1. 자바 실행 과정
1. **컴파일**: `javac` 컴파일러를 통해 소스 파일(`.java`)을 바이트 코드(`.class`)로 변환
2. **실행**: `java` 인터프리터(JVM)가 바이트 코드를 읽어 해당 OS에서 실행

---

## 2. JVM 메모리 구조
| 영역 | 특징 | 저장 데이터 |
| :--- | :--- | :--- |
| **Method 영역** | 클래스 정보 공유 공간 | 클래스 바이트 코드, `static` 변수 |
| **Heap 영역** | 동적 데이터 저장소 | **실제 객체(인스턴스)**, 배열, GC 관리 대상 |
| **Stack 영역** | 메서드 실행 공간 | **로컬 변수**, 매개변수, 연산 로직 수행 |

> **💡 메모리 할당 예시: `Car car = new Car("소나타");`**
> - **Stack**: 참조 변수 `car` 생성 (Heap에 생성된 객체의 참조값 저장)
> - **Heap**: 실제 `new Car("소나타")` 객체 데이터 저장

---

## 3. 변수의 분류

### ① 선언 위치에 따른 구분
* **멤버 변수 (Member Variable)**
  - 클래스 `{}` 내부에 선언
  - 객체의 구성 요소로 **Heap** 영역에 생성
* **로컬 변수 (Local Variable)**
  - 메서드, 생성자 등 `{}` 블록 내부에 선언
  - **Stack** 영역에 생성되며, 메서드 종료 시 자동 소멸

### ② 타입에 따른 구분
* **기본 타입 (Primitive Type)**: `char`, `byte`, `short`, `int`, `long`, `float`, `double`, `boolean`
* **참조 타입 (Reference Type)**: 기본 타입을 제외한 모든 타입 (객체, 배열 등 주소를 참조하는 타입)

---

## 4. java.lang.Object
- 자바의 최상위 클래스로, 모든 객체는 이 클래스를 자동으로 상속받음

---

## 5. 객체 생성 및 초기화 순서
클래스가 멤버 변수로 다른 클래스를 포함하고 있을 때의 생성 흐름

```java
class A { }

class B {
    A a = new A();
}

class C {
    B b = new B();
}

public static void main(String[] args) {
    C c = new C();
}
```
A -> B -> C
